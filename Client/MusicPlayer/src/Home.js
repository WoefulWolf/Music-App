import React from 'react';
import {
  Text,
  View,
  TouchableOpacity,
  TouchableOpacityComponent,
} from 'react-native';

const Home = () => {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
      }}>
      <Text>Hello, world!</Text>
    </View>
  );
};
export default Home;
